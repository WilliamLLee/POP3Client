import sys

from ui.FrameWindow import *
from config import *
from config import *

if __name__ == '__main__':
    # app = QApplication(sys.argv)
    # frame = FrameWindow()
    # frame.show()
    # sys.exit(app.exec_())
    print_msg()